name: Deploy to GCP Compute Engine (Staging & Production)

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'docker-compose.yaml'
      - 'deploy.sh'
      - 'init-letsencrypt.sh'
      - 'renew-cert.sh'
      - 'mysql/init/**'
      - 'nginx/**'
      - '.github/workflows/deploy-gcp.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'docker-compose.yaml'
      - 'deploy.sh'
      - 'init-letsencrypt.sh'
      - 'renew-cert.sh'
      - 'mysql/init/**'
      - 'nginx/**'
      - '.github/workflows/deploy-gcp.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to ${{ github.ref_name || github.base_ref }} Server
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set deployment environment
        id: set-env
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" || "${{ github.event.pull_request.base.ref }}" == "main" ]]; then
            echo "HOST=${{ secrets.GCP_VM_PROD_HOST }}" >> $GITHUB_ENV
            echo "USER=${{ secrets.GCP_VM_PROD_USER }}" >> $GITHUB_ENV
            echo "SSH_KEY=${{ secrets.GCP_VM_PROD_SSH_KEY }}" >> $GITHUB_ENV
            echo "TARGET_DIR=GAMERS-INFRA-PROD" >> $GITHUB_ENV
          
            echo "ENV_FILE<<EOF" >> $GITHUB_ENV
            echo "${{ secrets.PROD_ENV_FILE }}" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          else
            echo "HOST=${{ secrets.GCP_VM_STAGING_HOST }}" >> $GITHUB_ENV
            echo "USER=${{ secrets.GCP_VM_STAGING_USER }}" >> $GITHUB_ENV
            echo "SSH_KEY=${{ secrets.GCP_VM_STAGING_SSH_KEY }}" >> $GITHUB_ENV
            echo "TARGET_DIR=GAMERS-INFRA-STAGING" >> $GITHUB_ENV

            echo "ENV_FILE<<EOF" >> $GITHUB_ENV
            echo "${{ secrets.STAGING_ENV_FILE }}" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          fi

      - name: Copy files to GCP VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USER }}
          key: ${{ env.SSH_KEY }}
          port: 22
          source: "docker-compose.yaml,deploy.sh,init-letsencrypt.sh,renew-cert.sh,Makefile,nginx,mysql"
          target: ${{ env.TARGET_DIR }}
          strip_components: 0

      - name: Execute SSH command to deploy
        uses: appleboy/ssh-action@v1.0.3
        env:
          GHCR_PAT: ${{ secrets.GHCR_PAT }}
          GITHUB_ACTOR: ${{ github.actor }}
          ENV_FILE: ${{ env.ENV_FILE }}
          TARGET_DIR: ${{ env.TARGET_DIR }}
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USER }}
          key: ${{ env.SSH_KEY }}
          port: 22
          envs: GHCR_PAT,GITHUB_ACTOR,ENV_FILE,TARGET_DIR
          script: |
            cd $TARGET_DIR

            echo "📝 Generating .env file..."
            echo "$ENV_FILE" > .env
            echo "✅ .env file created."
            
            if [ -z "$GHCR_PAT" ]; then
              echo "❌ Error: GHCR_PAT secret is empty."
              exit 1
            fi
            
            echo "🔐 Logging in to GHCR..."
            echo "$GHCR_PAT" | docker login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            
            chmod +x deploy.sh
            ./deploy.sh